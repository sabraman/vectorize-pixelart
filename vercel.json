{
  "version": 2,
  "buildCommand": "cd web && pnpm install --frozen-lockfile && pnpm build",
  "devCommand": "cd web && pnpm dev",
  "outputDirectory": "web/.next",
  "installCommand": "cd web && pnpm install --frozen-lockfile",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/web/$1"
    }
  ],
  "env": {
    "NEXT_PUBLIC_APP_NAME": "Vectorize Pixelart"
  }
}

